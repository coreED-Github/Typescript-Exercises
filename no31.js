"use strict";
let username = ['admin', 'tania', 'saima', 'mahira', 'laiba'];
if (username.length === 0) {
    console.log('we need to find some users!');
}
else {
    username = [];
    console.log('all user name has been removed.' + username.length);
}
